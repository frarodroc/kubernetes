apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-secrets.fullname" . }}
  labels:
    {{- include "hello-secrets.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-secrets.selectorLabels" . | nindent 4 }}
