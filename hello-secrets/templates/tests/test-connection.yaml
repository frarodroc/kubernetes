apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-secrets.fullname" . }}-test-connection"
  labels:
    {{- include "hello-secrets.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-secrets.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
